first cd to the root folder
then run npm update
after that run npm run bundle 
then you will be able to start the http server with the following command:
http-server . -p3000
then navigate to the following address on you browser to use the website:
127.0.0.1:3000
